# API Cadastro de Alunos

* Para utilizar a API é necessário ter o NPM instalado na máquina
* Após fazer o download do ZIP ou clone em uma pasta, abra o terminal
* Após abrir o terminal execute o comando ``` npm install ```
* após concluir, execute o comando ``` npm start ```
